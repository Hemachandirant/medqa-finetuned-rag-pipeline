# ─────────────────────────────────────────────
# Core RAG Framework
# ─────────────────────────────────────────────
langchain==0.3.25
langchain-community==0.3.24
langchain-core==0.3.60
langchain-text-splitters==0.3.8

# ─────────────────────────────────────────────
# Vector Store — Pinecone
# (langchain-openai needed for AzureOpenAIEmbeddings used in rag_pipeline.py + evaluate.py)
# ─────────────────────────────────────────────
langchain-openai==0.3.16
langchain-pinecone==0.2.4
pinecone-client==5.0.1

# ─────────────────────────────────────────────
# AWS SageMaker (LLM inference)
# boto3: SageMaker runtime client in SageMakerLLM._call()
# sagemaker: deployment scripts under model deployment/
# ─────────────────────────────────────────────
boto3==1.38.0
sagemaker==2.242.0

# ─────────────────────────────────────────────
# Document Loading
# pypdf: PDF loading via PyPDFLoader in index_documents.py
# ─────────────────────────────────────────────
pypdf==5.4.0

# ─────────────────────────────────────────────
# RAGAs Evaluation
# Pinned to vibrantlabsai fork (v0.4 API used in evaluate.py:
#   SingleTurnSample, single_turn_ascore, LangchainLLMWrapper, LangchainEmbeddingsWrapper)
# ─────────────────────────────────────────────
git+https://github.com/vibrantlabsai/ragas
datasets==3.5.1
pandas==2.2.3

# ─────────────────────────────────────────────
# API Server (main.py / FastAPI)
# ─────────────────────────────────────────────
fastapi==0.115.12
uvicorn==0.34.2
pydantic==2.11.4

# ─────────────────────────────────────────────
# Utilities
# ─────────────────────────────────────────────
python-dotenv==1.1.0
tqdm==4.67.1